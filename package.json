{
  "dependencies": {
    "cors": "^2.8.5",
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.10"
  },
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm install && npm run dev",
    "client": "cd staff-interview-ui && npm install && npm run dev"
  }
}
